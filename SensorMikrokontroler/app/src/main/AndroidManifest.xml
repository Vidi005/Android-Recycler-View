<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.submission.dicoding.sensormikrokontroler">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/android_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/android_logo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity android:name=".SplashScreen"
            android:label="@string/app_name"
            android:noHistory="true"
            android:screenOrientation="portrait"
            android:theme="@style/AppThemeSplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AboutActivity" />
        <activity android:name=".ListSensorDetail" />
        <activity android:name=".ListSensorsAdapter" />
        <activity android:name=".MainActivity"/>
    </application>

</manifest>